
#include "main.h"

/**
 * more_numbers - cheack if the character is upper or lower
 * Return: if uppercase 0 otherwise
*/

void more_numbers(void)
{
	int c;

	for (c = '0'; c <= '10'; c++)
	{
		for (c = '0'; c <= '14'; c++)
			_putchar(c);
		_putchar('\n');
	_putchar('\n');
	}
}
