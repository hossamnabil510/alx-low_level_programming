#include "main.h"

/**
 * mul - cheack if the character is upper or lower
 * @a: input char
 * @b: input char
 * @c: output char
 * Return: c if uppercase 0 otherwise
*/

int mul(int a, int b)
{
	return (a * b);
}
