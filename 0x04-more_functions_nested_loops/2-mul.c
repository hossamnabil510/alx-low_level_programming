#include "main.h"

/**
 * mul - cheack if the character is upper or lower
 * @a: input char
 * @b: input char
 * Return: if uppercase 0 otherwise
*/

int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
