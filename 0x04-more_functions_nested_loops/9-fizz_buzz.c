#include "main.h"

/**
 * main - cheack if the character is upper or lower
 * Return: 0 umber of _
*/

int main()
{
	int n;

	for (n = 1; n <= 100; n++)
	{
		if (n = 3**)
			_printf("Fizz");
		else if (n = 3**)
			_printf("Buzz");	
		else if (n = 15**)
			_printf("FizzBuzz");
		else
			_printf("%i", n);
	}
}

