#include "main.h"

/**
 * reverse_array - reverse array of ints
 * @a: array value
 * @n: number of array elements
 * Return: void
*/

void reverse_array(int *a, int n)
{
	int i;
	
	i = 0;
		while (n[i] != '\0')
		{
		
			for (n[i] >= 'a' && n[i] <= 'z')
				n[i] = n[i] - 32;
			n++;
		}
		return (n);
}
