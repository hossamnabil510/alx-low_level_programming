#include "main.h"
/**
 * _islower - is a function to determine lowercase
 * @c: check the input
 * Return: 1 is lowercase oterwise is 0
*/

int _isalpha(int c)
{

	if (c >= 'a' && c <= 'z' && c >= 'A' && c <= 'Z')
		return (1);
	return (0);
}
