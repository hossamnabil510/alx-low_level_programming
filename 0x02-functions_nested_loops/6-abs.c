#include "main.h"
/**
 * _abs - is a function to determine positive and negative nums
 * @int: check the input
 * Return: 1 is lowercase or  oterwise is 0
*/

int _abs(int)
{

	if (r >= 0)
	{
		_putchar(r);
	}
	else
		printf("%i", -1 * r);
	return (0);

}
